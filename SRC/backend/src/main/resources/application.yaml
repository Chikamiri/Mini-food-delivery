spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini_food_db?createDatabaseIfNotExist=true
    username: root
    password: 2142005
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: "ExampleSecretKeyForJWTGeneration"
    expiration-ms: 86400000 # 24 hours

